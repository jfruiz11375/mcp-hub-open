# Roadmap

## Included now
- local auth + JWT + RBAC
- encrypted secret vault for env vars
- Docker isolation mode
- basic multi-node registry and heartbeats
- experimental MCP JSON-RPC proxy endpoint
- checksum and detached-signature verification hooks

## Next
- true remote worker agent for nodes
- verified OIDC ID token validation
- session-aware streaming MCP proxy transport
- audit logs and approvals
- database-backed persistence
- packaged signed releases with CI
